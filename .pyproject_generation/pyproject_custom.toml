[project]
name = "fsb"
version = "1.0.0"
description = "File Services Backend - monorepo housing file services"
dependencies = [
    "typer >= 0.15",
    "ghga-service-commons[api, auth, crypt] >= 4.0",
    "ghga-event-schemas >= 5.0",
    "hexkit[akafka, s3, mongodb, opentelemetry] >= 4.3.0",
    "crypt4gh >= 1.7",
    "hvac >= 2",
    "httpx >= 0.28",
]

[project.urls]
Repository = "https://github.com/ghga-de/file-services-backend"
